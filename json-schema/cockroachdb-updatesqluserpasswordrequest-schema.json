{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "#/components/schemas/UpdateSQLUserPasswordRequest",
  "title": "UpdateSQLUserPasswordRequest",
  "type": "object",
  "description": "Request body for updating a SQL user's password.",
  "required": [
    "password"
  ],
  "properties": {
    "password": {
      "type": "string",
      "description": "New password for the SQL user.",
      "format": "password"
    }
  }
}